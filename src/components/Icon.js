import MaterialCommunityIcons from 'react-native-vector-icons/MaterialCommunityIcons';

export function Icon({name, style}) {
  return <MaterialCommunityIcons name={name} size={25} style={style} />;
}
